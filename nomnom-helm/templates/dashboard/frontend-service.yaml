{{- if .Values.dashboard.frontend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nomnom.dashboard-frontend.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "nomnom.dashboard-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.dashboard.frontend.service.type }}
  selector:
    {{- include "nomnom.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: dashboard-frontend
  ports:
  - name: http
    port: {{ .Values.dashboard.frontend.service.port }}
    targetPort: http
    protocol: TCP
{{- end }}
