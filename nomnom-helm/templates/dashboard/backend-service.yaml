{{- if .Values.dashboard.backend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nomnom.dashboard-backend.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "nomnom.dashboard-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.dashboard.backend.service.type }}
  selector:
    {{- include "nomnom.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: dashboard-backend
  ports:
  - name: http
    port: {{ .Values.dashboard.backend.service.port }}
    targetPort: http
    protocol: TCP
{{- end }}
